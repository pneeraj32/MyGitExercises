# Hello GitHub

Updating the file with cloning steps
-To clone a repository, use the git clone <url> command replacing <url> with the URL of the repository to clone.
-use commands like -> git add . - to add any files into staging area, git commit -m "message" to commit any changes and git push to push the changes on to local Github repository

